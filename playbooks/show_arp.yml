---
- name: Show ARP table
  hosts: gns3_routers
  gather_facts: false

  tasks:
    - name: Show ARP
      cisco.ios.ios_command:
        commands:
          - show ip arp
      register: output

    - name: Display ARP
      ansible.builtin.debug:
        var: output.stdout_lines