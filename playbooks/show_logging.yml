---
- name: Show logging
  hosts: gns3_routers
  gather_facts: false

  tasks:
    - name: Show logging
      cisco.ios.ios_command:
        commands:
          - show logging
      register: output

    - name: Display logging
      ansible.builtin.debug:
        var: output.stdout_lines