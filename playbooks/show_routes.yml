---
- name: Show IP routes
  hosts: gns3_routers
  gather_facts: false

  tasks:
    - name: Show routes
      cisco.ios.ios_command:
        commands:
          - show ip route
      register: output

    - name: Display routes
      ansible.builtin.debug:
        var: output.stdout_lines