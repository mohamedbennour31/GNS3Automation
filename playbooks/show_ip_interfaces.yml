---
- name: Show IP interface details
  hosts: gns3_routers
  gather_facts: false

  tasks:
    - name: Show IP interfaces
      cisco.ios.ios_command:
        commands:
          - show ip interface
      register: output

    - name: Display IP interface details
      ansible.builtin.debug:
        var: output.stdout_lines